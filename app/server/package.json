{
    "name": "my-app",
    "scripts": {
        "dev": "bun run --hot src/index.ts",
        "prisma": "prisma",
        "prisma:generate": "prisma generate --schema=src/db/user.schema.prisma",
        "prisma:migrate": "prisma migrate dev --schema=src/db/user.schema.prisma",
        "prisma:studio": "prisma studio --schema=src/db/user.schema.prisma"
    },
    "dependencies": {
        "@prisma/client": "6.7.0",
        "hono": "^4.7.9",
        "prisma": "^6.7.0"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
